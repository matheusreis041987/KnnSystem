spring.datasource.url=jdbc:h2:mem:db_KnnSystem;INIT=RUNSCRIPT FROM 'src/main/resources/h2init.sql';
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=

api.security.token.secret=${TOKEN_SECRET:test-token-secret}
api.security.token.issuer=${TOKEN_ISSUER:test-token-issuer}